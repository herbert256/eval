# Eval bug hunt — 14 September 2026

Ran the app on `emulator-5554` (Android API 36.1) with the installed Stockfish engine. Changes preserve the existing screen layout.

Historical references to a retired online source were removed on 16 September; the original test totals below still include that former coverage.

## Fixed findings

| Area | Reproduction / previous behavior | Correction |
| --- | --- | --- |
| Coordinate move import | `g1f3` was accepted as pawn SAN and moved the f-pawn; the existing live-navigation test failed. | Separate SAN/UCI validation, with legal-move and promotion checks before changing the board. |
| SAN piece selection | `Nf4` could move a pinned knight; explicit source-file disambiguation was ignored when only one candidate existed. | Select the unique legal source piece and respect disambiguation. Castling, en passant, and underpromotion have regression coverage. |
| Annotated PGN | Moves inside comments/variations could enter the main line; separate evaluation and clock comments lost clocks; malformed tokens were silently skipped. | Tokenize the main line, preserve clocks, ignore nested variations and comments, and report the first invalid move. Supports attached move numbers, NAGs, BOMs, and castling suffixes. |
| Manual variations | Play e4/e5, rewind, then play c5: the variation became e4/e5/c5 and navigation used the abandoned board. | Truncate the future moves and snapshots when branching. |
| FEN studies and custom PGN | FEN entry retained the previous game's history; returning from exploration or reopening the app could show the wrong position. Imports/exports assumed the normal initial position. | Reset and persist the correct initial board; honor PGN FEN/SetUp in loading, restoration, PGN export, and GIF export. Keep Black-to-move numbering and move colors correct. Invalid FEN remains editable with an inline error. |
| GIF sharing | Encoding succeeded but sharing failed with `Failed to find configured root ... /cache/game_annotated_....gif`. | Generate GIFs in a dedicated `gif_exports` folder exposed by FileProvider. The emulator test reads and decodes the generated content URI without sending it anywhere. |
| Engine startup | A cold start exceeded the old handshake timeout, while `initialize()` still returned success; the emulator startup test timed out. | Allow a bounded 15-second UCI handshake, return actual readiness, serialize initialization/restarts, propagate cancellation, detach closed streams during cleanup, and reuse the ready engine when restoring a saved game instead of unnecessarily restarting it. Restored-position analysis is tracked so replacing a game cancels the old request. |

## Verification

- 15 JVM tests passed, including the previously failing live-navigation test.
- 15 Android instrumentation tests passed. After the final engine-restore correction, all five affected emulator scenarios passed again, including live evaluation after reopening a FEN study.
- Real Stockfish Preview → Analyse → Manual completed for an annotated game and for a custom starting position.
- Retrieved public games from Lichess (`DrNykterstein`).
- Verified FEN navigation and restoration through a new Activity/ViewModel instance.
- Generated a three-frame GIF, resolved its share URI, and decoded the result.
- Existing pagination, retry, AI handoff/migration, and settings round-trip tests passed.
- Debug APK build and Android lint passed; lint reported zero errors and 88 warnings.
- Visually inspected the board, correct knight positions, Black-to-move move numbering, the FEN study, and settings navigation.

Commands:

```sh
JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/Users/herbert/Library/Android/sdk ./gradlew :app:assembleDebug :app:testDebugUnitTest :app:assembleDebugAndroidTest :app:lintDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb install -r app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk
adb shell am instrument -w -r com.eval.test/androidx.test.runner.AndroidJUnitRunner
```

The runtime scenarios use public data and isolated fixtures. They require the companion Stockfish package; the network retrieval scenario also requires internet access. This was an emulator pass, not a test of every device or every live content source. Existing uncommitted work was preserved.

The final APK was installed and Eval was verified in the foreground. Original app preferences (including saved games) were restored byte-for-byte before the final launch. No commit or push was performed.

## Continued testing pass

Five additional findings were reproduced and fixed without changing the UI layout:

| Area | Reproduction / previous behavior | Correction |
| --- | --- | --- |
| Settings on startup | Before the first remote game retrieval, or when the saved version differed, startup cleared preferences. This erased local settings, FEN history, AI instructions, and saved games. | Use existing preference defaults for missing values; remove the destructive startup reset. Both version cases have emulator regression coverage. |
| Settings import and game storage | Export settings, then import them: the current game, analysed-game collection, and retrieved-game history disappeared because they share the preferences file. | Replace only settings, retaining game storage. Typed and legacy imports are covered. Resetting settings also preserves games. |
| Invalid settings files | Importing `{}` succeeded and reset settings because Gson supplied snapshot defaults. Unknown versions and unsupported legacy types could also be accepted. | Require a supported explicit typed version or a nonempty legacy map with supported types. Invalid imports return failure without applying changes. Existing v2 migration and v3 round-trip tests still pass. |
| GIF cancellation | Cancel after the first frame: encoding continued and opened the share chooser afterward. | Cancel the export job, check cancellation between frames, remove incomplete files, and treat cancellation separately from errors. Serialize encoders to protect shared drawing objects and reject duplicate active exports. |
| Switching saved-game lists | Opening a shorter list from page two showed an empty page. A pending response could then overwrite the newly selected list with the old account's games. | Reset paging when selecting/reopening a list, and ignore responses from an obsolete selection. Tests cover both saved-list entry paths and an outstanding page request. |

Verification for this pass:

- Baseline emulator reproduction: six failing tests across the five findings; the separate pending-request test also failed before its fix.
- All 15 JVM tests passed.
- The full 21-test Android suite passed. After the final pending-request guard, all eight affected Android tests passed, covering six pagination cases, GIF cancellation, and successful custom-position analysis/GIF sharing preparation. There are now 22 Android tests in total.
- Debug build and lint passed again: zero lint errors, 88 warnings.
- Manually opened Settings and General, changed Move sounds, force-stopped/relaunched Eval, and verified the disabled value both on screen and in persisted preferences.
- Live public retrieval from Lichess and actual Stockfish analysis passed in the full suite. Share tests intercepted the chooser intent; nothing was sent to another person.

Fresh backup and raw reproduction/build/test logs are in `/tmp/eval-continued/` on this machine. This pass preserved the pre-existing source changes.

After the manual persistence check, the original preferences were restored byte-for-byte again. The final cold launch showed the original DrNykterstein–yoseph2013 game with live Stockfish analysis at depth 14. The app was left running; changes remain uncommitted.

## Third testing pass

| Area | Reproduced failure | Correction |
| --- | --- | --- |
| PGN collections and broadcasts | Games disappeared when separated by one newline, indented blank lines, or a header other than Event. Event text inside a comment created a phantom game. | A shared scanner identifies headers, comments, main-line moves, variations, and result boundaries. Both local files and broadcast PGNs use it. Tests cover LF/CRLF, indentation, different header order, headerless games, and comment contents. |
| Imported player metadata | A quoted player name became `White`; a tag written inside a comment could become a player name. | Use the shared header parser, including escaped quotes and backslashes, and ignore comment contents. |
| Draw results | An imported `1/2-1/2` remained a raw status the UI/export did not recognize; export changed it to `*`. A headerless draw lost its result entirely. | Normalize imported draws and read the main-line result when the Result header is absent. PGN export also recognizes old raw draw statuses. |
| Exported evaluations | Under a comma-decimal locale, a score was written as `+1,25`; mates used the UI-only `-M3` notation. | Write locale-independent decimal values and `#-3` mate annotations. Regression tests check both formats. |
| Live-stream completion | A final game-description message parsed successfully as an empty move, so no game-end event reached the app. One-time retrieval also returned completed games as `started`. | Inspect status before interpreting a message as a move, including nested `status.name`. Both continuous following and one-time retrieval preserve wins/draws, including the generated PGN. Stream cancellation propagates separately from parse errors. |
| Engine readiness and early game loading | The first full emulator run exposed two analysis timeouts. Logs showed a live engine marked unavailable after the three-second `readyok` wait; `startAnalysis()` silently returned when readiness was false. | Use a bounded 15-second readiness deadline and matching allowances in analysis/watchdog waits. Queue a newly loaded game until initialization completes, with a bounded restart fallback. Startup restoration also avoids replacing a game already selected. |

The live-stream fixture follows the [Lichess stream endpoint specification](https://raw.githubusercontent.com/lichess-org/api/master/doc/specs/tags/games/api-stream-game-id.yaml) and [message example](https://raw.githubusercontent.com/lichess-org/api/master/doc/specs/schemas/MoveStream.yaml). Evaluation export formatting was checked against the [python-chess PGN reader and writer](https://github.com/niklasf/python-chess/blob/master/chess/pgn.py).

Before correction, both new evaluation unit tests failed, and all six new repository/stream emulator tests failed. Logs and a fresh app-data backup are in `/tmp/eval-continued-2/`.

The first 29-test full emulator run passed the PGN and continuous-stream regressions but exposed two engine-readiness failures. The extended one-time stream retrieval assertion separately reproduced the lost completion status. A new test creates the ViewModel and loads a game before Stockfish is ready, then requires both analysis stages to finish.

Final verification:

- All 17 JVM tests passed; debug build and lint passed with zero errors and 88 warnings.
- On the final app code, 29 of 30 Android tests passed in the full run. The new startup test initially checked readiness during the transition to Manual; after making it wait for a live evaluation of the current FEN, it passed separately in 18.234 seconds. All 30 current Android cases have therefore passed on the final app code.
- The end-to-end collection test selected and analysed both imported games. The captured UI showed Carol and Dave each with a half-point result.
- Public Lichess retrieval, real Stockfish analysis, FEN restoration, GIF completion/cancellation, pagination, settings preservation, and AI handoff/migration regressions passed.
- Live-game completion tests cover both wins and draws through continuous following and one-time retrieval using recorded-format fixtures. They do not depend on waiting for an actual online game to finish.
- Original settings and saved games were restored byte-for-byte from this pass's fresh backup. Existing source changes were preserved, and no commit or push was performed.
- Final cold launch restored the original game and showed live Stockfish analysis at depth 13. The final process log contained no app-crash or Stockfish-error entries, and the app was left running.


## Fourth testing pass

| Area | Reproduced failure | Correction |
| --- | --- | --- |
| Rapid board navigation | Three Next actions before engine work ran left the board at the old index. Navigation updated the selection only after the previous analysis job finished cancelling, so repeated taps reused the same index. | Update the board, move index, opening name, and analysis request identity immediately. Clear stale results immediately; engine cleanup and analysis remain asynchronous. |
| Shared position identity | A Black-to-move FEN on move 17 was labelled using the main game's old index. The initial position was described as after move zero. | Derive the side to move and full-move number from the actual shared board, including explored variations. |
| Shared evaluation | Sharing immediately after changing position attached the previous position's evaluation and best move. Black-to-move scores lacked a stated perspective, and mate text lost which side was winning. | Include analysis only when its FEN matches the shared board. Express numeric scores from White's perspective, using a locale-independent decimal point, and name the mating side. |
| Invalid settings imports | Legacy imports accepted a Boolean for the username, fractional integer settings, negative MultiPV, and invalid graph scales. Typed imports accepted invalid ranges and coerced primitive types. These values could break preference reads, rendering, or engine configuration. | Validate typed structure and legacy preference types before writing. Enforce the supported numeric ranges for engine and display settings, reject non-finite/overflow/fractional integer values, and validate FEN/AI list contents. Rejected imports leave settings and saved games untouched. Legacy numeric parsing also retains Long precision. |

Before these corrections, six regression cases failed in a 10-test emulator reproduction run. The navigation state test deliberately prevents engine jobs from running, making dropped input reproducible without timing assumptions. A separate end-to-end test exercises repeated Next/Previous actions and board flipping with real Stockfish, then waits for an evaluation of the final selected FEN. Share tests intercept the chooser intent and do not send messages.

This pass's fresh backup, build logs, and emulator results are stored in `/tmp/eval-continued-3/`. Existing uncommitted work is preserved. No UI layout changes were made.


The manual Android share-preview check exposed an additional ANR after the initial 37-test suite and final eight-case settings recheck had passed. At 14:02:42, Android timed out waiting for Eval to process loss of window focus. The captured main-thread stack was in `Bitmap.createScaledBitmap` through `Drawable.toBitmap`, inside `ChessBoardView.kt:90`. Returning from the full-screen Share / Export view recreated the board and synchronously decoded/scaled all twelve piece images while Stockfish and the share chooser were active. The resulting frame took approximately 20 seconds.

The board now loads the existing piece assets on an IO dispatcher, at their original 480-pixel resolution, and shares the images across board instances. Returning from an overlay uses the populated cache immediately. Piece artwork and board layout are unchanged. An emulator regression checks that concurrent board loads share all twelve native-resolution images instead of allocating density-enlarged copies.

ANR evidence: `share-lastanr.txt`, `share-anr-dropbox.txt`, `share-anr-logcat.txt`, and `android-share-preview.png` in `/tmp/eval-continued-3/`.


Retesting after image caching still found focus timeouts, now in layout measurement and graph drawing. The final correction also retains the game composition beneath Share / Export, avoiding a full board/PV/move-list reconstruction when the chooser opens. Investigation confirmed that the emulator had one online CPU while Stockfish was configured for four normal-priority workers. Engine workers are now capped to the available CPU count and launched with lower scheduling priority using Android's `nice` utility where available. The saved settings are unchanged. Live process inspection confirmed a single configured worker at niceness 10; repeated real share-chooser checks then produced no new ANR while the engine continued through depths 23 and 30.


Final verification for the fourth pass:

- The complete 38-test Android suite passed on the final app code in 134.43 seconds (`settled-emulator-tests.txt`). All 17 JVM tests passed; the debug build and lint passed with zero errors and 88 warnings.
- Repeated the actual Android Share Position chooser from the original 59-move game after rapid navigation. The preview correctly showed Black to move on move 37 and an evaluation labelled from White's perspective. The chooser was dismissed without sending anything. Android recorded no new ANR after the completed fix; its last ANR remained the earlier 14:14:33 reproduction.
- The image-cache regression verified twelve shared 480 x 480 images. Visual checks confirmed the original piece artwork, board layout, and Share / Export screen.
- Restored the original preferences and saved games byte-for-byte, removed only test-created screenshots, and cold-launched the installed app. The original game was visible at move 35/59 with live Stockfish analysis at depth 9. The final process log contained no app-crash or Stockfish-error entries.
- The app was left running. Source changes remain uncommitted, and existing work was preserved.

## Fifth testing pass

| Area | Reproduced failure | Correction |
| --- | --- | --- |
| Switching games while a request is pending | A delayed download replaced a newly imported local game or appeared behind an imported PGN collection. Reloading could reopen a cleared game, and an older reload could win after a newer one had finished. A reload queued immediately before a local selection also overwrote that selection. | All game retrievals share a selection generation. Reload captures its generation when requested and checks it before starting and after receiving a response. Loading a local/saved game, importing a collection, clearing the game, starting from FEN, and returning home invalidate pending retrievals and reset retrieval loading state. |
| Dragging a piece outside the board | Actual touch injection moved a rook from d4 to a4 after it was released outside the left edge. With the board flipped, the same gesture moved it to h4. | Reject pointer coordinates outside the board before converting them to squares. An outside release cancels the move; releasing inside the edge still makes the legal rook move. |
| Imported game links | Uppercase `LICHESS.ORG` was rejected, while bare domain strings, `javascript:lichess.org`, lookalike hosts, and an unrelated site's path containing `lichess.org` were accepted. Browser launch errors were unhandled. | Parse absolute HTTP/HTTPS URLs and validate their host case-insensitively against the supported domains/subdomains. Reject embedded credentials and malformed/unsupported links. Use the same host interpretation for the button caption and AI server metadata. Missing or blocked browser handlers produce the existing error message presentation instead of an uncaught exception. |

The initial seven emulator regressions all failed: five deterministic delayed/queued retrieval cases and two real touch-gesture cases. After their corrections, all seven passed. The separate imported-link regression then reproduced the hostname/scheme failures and was corrected. Browser failure tests intercept launch calls, simulate missing/blocked handlers, and verify that a normal launch receives the expected URL while retaining the selected game.

Artifacts and the fresh app-data backup for this pass are in `/tmp/eval-continued-4/`: `baseline-tests.txt`, `focused-tests.txt`, `validation-build.txt`, and `full-emulator-tests.txt`. The first full validation build passed all 20 JVM tests and lint (zero errors, 88 existing warnings). Existing uncommitted work is preserved; the visual layout is unchanged.

The same first full run exposed a test setup race in both new gesture tests: the main looper became idle before Compose reported the board bounds. The test now waits explicitly for measured bounds before injecting gestures. This changed test synchronization only; the earlier gesture reproduction and passing focused run remain recorded.

Final verification for the fifth pass:

- The complete 49-test Android suite passed on the final app code in 105.896 seconds (`settled-emulator-tests.txt`). Both live public providers returned games successfully. The suite includes Stockfish completion/startup, rapid navigation, both board orientations, local PGN/FEN workflows, settings safety, GIF/sharing, AI handoff, and the new retrieval/link regressions.
- All 20 JVM tests passed. The final debug build and lint passed with zero lint errors and 88 warnings. `git diff --check` passed.
- Restored the original settings and saved games byte-for-byte from this pass's fresh backup and removed only test-created screenshots. Cold launch showed the original DrNykterstein–yoseph2013 game at move 35/59, with live Stockfish analysis at depth 15.
- Used the installed app's View on lichess.org button. Chrome received the exact saved-game URL, `https://lichess.org/fNUpn0oK`, and displayed its board and move list. Browser-error tests separately covered unavailable and blocked handlers.
- Android recorded no new ANR during the complete final test run; its last ANR remained the earlier fourth-pass 14:14:33 reproduction. The final Eval process log had no fatal exception or Stockfish error entries.
- The app was left running with the original game. No visual layout changes, commits, or pushes were made; existing source work remains preserved.

## Sixth pass: saved-game state, opening information, and Manual-stage navigation

The fifth-pass changes were subsequently committed and pushed in Eval (`2cede21`) and AI (`a1d7de2d8`). Continued testing started from those clean checkouts. A fresh Eval data backup and this pass's evidence are in `/tmp/eval-continued-5/`.

Three deterministic saved-game regressions failed before correction (`baseline-tests.txt`). Restoring another saved game retained the previous game's live evaluation, opening statistics, and move classifications. Loading a new PGN retained opening metadata; clearing the game left its evaluation FEN and opening data. These transitions now clear derived position data, and restored classifications are recalculated from that game's saved scores. Restoring a saved game also closes the retrieval screen.

Opening lookups were scheduled after a continuous engine polling loop that normally does not return. Move-list taps and variation navigation did not request new opening data. The local opening book also received SAN strings even though its keys are UCI moves. Opening information now observes the displayed position and settings independently of engine progress. It debounces navigation, cancels obsolete requests, checks the selection and FEN before accepting a response, and clears obsolete data/loading/error state. Local names use the actual board-history moves, including variations; custom FEN studies do not inherit an unrelated opening. Fixtures cover delayed uncancellable responses, switching games at an identical FEN, reloading the same game, hiding the panel, request failures, and entering/leaving a variation.

The first live opening test exposed a separate engine lifecycle race. Manual-stage entry launched an untracked coroutine, allowing immediate navigation to start another search while the original coroutine restarted/reconfigured Stockfish. The stage-entry job is now owned by `manualAnalysisJob`, so navigation cancels it. Request identities are checked before searches start, readiness comes directly from the engine, and stopping invalidates the previous request. Engine results carry their originating FEN, which is checked before displaying or storing them. The regression deliberately navigates as Manual mode begins, before waiting for engine readiness; it passed after correction (`live-retest.txt`).

Live opening retrieval exposed an external dependency change: the public endpoint returns HTTP 401, confirmed independently in `explorer-http-headers.txt`. [Lichess's changelog](https://lichess.org/page/changelog) confirms that opening-explorer endpoints now require authentication. Eval previously presented this as “No opening data available.” The existing panel now displays the authentication failure or other request error, while offline opening names continue working. In-app authenticated statistics remain unavailable; no authentication flow or credentials were added. The live regression accepts a successful response or the explicit authentication requirement, and still requires engine analysis of the selected FEN. Successful and failed data delivery are also covered by deterministic fixtures.

An initial real Eval-to-AI handoff reached saved-prompt selection, system-prompt selection, and the external-request confirmation with the correct game title. It was cancelled before report generation, and the Eval process survived. Screenshots are saved as `ai-handoff-screen.png`, `ai-prompt-choice.png`, and `ai-confirmation.png`. That initial check used a completed depth-32 search; it does not establish responsiveness during an active search.

Final verification for the sixth pass:

- The complete 62-test Android run finished in 303.014 seconds: 60 passed, and the two live-network checks failed during an emulator DNS outage (`full-emulator-tests.txt`). Public-game retrieval reported `UnknownHostException`, and even Android's system `ping` could not resolve `lichess.org`, while direct IP connectivity worked. The emulator was restarted with `-dns-server 1.1.1.1`, then its Wi-Fi connection was cycled and restored. Both affected live checks subsequently passed in 16.36 seconds (`recovered-live-tests.txt`), including retrieval from Lichess. All 62 test cases therefore have passing results; the initial failed run is retained rather than described as a clean single run.
- All 20 JVM tests passed, the debug/test APK builds passed, and lint reported zero errors and 88 warnings. `git diff --check` passed. The final test-only edit improves failure diagnostics; production code was unchanged between the full run and live-network retest.
- `verified-opening-panel.png` shows the correct offline opening name, live Stockfish depth 9, and the explicit Lichess authentication message. Successful opening-statistics delivery is fixture-tested; live unauthenticated statistics remain unavailable.
- Restored the original app-data archive and verified `eval_prefs.xml` byte-for-byte against the fresh backup. Removed only this pass's temporary screenshots and test preferences. Cold launch restored DrNykterstein–yoseph2013 at move 35/59.
- Moved to 36/59 and opened Share while its search was at depth 6 (`active-share-screen.png`). The existing position-coach instruction reached AI's prompt/system selection and external-request confirmation, then was cancelled before generation (`active-ai-confirmation.png`). Eval resumed in the same process, PID 4913, with the selected board and completed depth-32 analysis (`returned-eval.png`). No reports were generated during these checks.
- Android recorded no ANR during the full suite or final handoff (`full-run-lastanr.txt`, `active-handoff-lastanr.txt`). The final Eval process log contained no fatal exception or engine error. Existing layout and settings defaults remain unchanged.

## Seventh pass: variations, move quality, and live-game state

This pass started from Eval `80c4dcd` and AI `a1d7de2d8`, both clean and pushed. A fresh app-data archive and all reproduction/verification logs are in `/tmp/eval-continued-6/`.

| Area | Reproduced failure | Correction |
| --- | --- | --- |
| Nested variations | Selecting a new engine continuation inside a variation discarded the moves already played in that variation. Selecting an invalid stale continuation erased the existing branch. | Preserve the branch prefix and original main-game return position, replace only the abandoned future, and validate the new UCI continuation before changing state. |
| Immediate variation evaluation | Moving into a variation left the previous position's evaluation visible until engine cleanup ran. | Invalidate the request and clear the old result synchronously, before asynchronous engine work starts. |
| Variation position label | Screenshot review showed “Start position” above the board after e4, e5, Nc3. Variations entered later in a game instead mixed the main game's move/score metadata with variation coordinates. | The same result bar now identifies the variation's actual full-move number and side to move. The surrounding layout is unchanged. |
| Move-quality attribution | Classifications compared positions two plies apart. A player could give back a five-pawn advantage without receiving a blunder label, or receive credit for the opponent's preceding mistake. | Compare the position immediately before and after each move, using the actual mover from board history. Missing adjacent scores remain unclassified; existing quality thresholds are unchanged. Black-to-move custom FENs are covered. |
| Late live responses | A pending response appended a move to a newly loaded local game or repopulated a cleared game. It could also change the new game's result. | Stop following when changing/clearing games and invalidate old stream sessions before accepting events. |
| Live board and clocks | Live play reconstructed custom FEN games from the standard starting position. Following a move replaced an explored board while leaving variation mode active. | Replay from the game's initial FEN, use the actual piece colour for clock attribution, and extend the main history without moving an explored position. |
| Reconnected stream history | A loaded Nf3, Nf6, Ng1, Ng8 sequence was appended a second time when replayed by a new connection. | Consume and validate the already loaded history before appending new moves. The fixture follows Lichess's [documented stream example](https://raw.githubusercontent.com/lichess-org/api/master/doc/specs/schemas/MoveStream.yaml). |
| Live evaluation | Auto-following a new move retained the previous board's analysis and requested no replacement. | Clear the stale evaluation and request analysis of the newly displayed board. Incoming main-game moves leave an explored position's analysis alone. |

The first 13-case focused run reproduced 11 failures (`baseline-tests.txt`), then all 13 passed after correction (`focused-tests.txt`). Two additional live-stream cases independently reproduced duplicate replayed moves and stale live evaluation (`live-baseline-tests.txt`). A real Stockfish test also follows a nested variation, navigates its full retained history, returns to the unchanged main game, and requires matching-FEN evaluations on both sides of that transition.

Live-manager tests use controlled NDJSON streams through the real repository parser. They verify cancellation, history, clocks, and state transitions without relying on the timing of an ongoing online game. The app currently exposes no UI call that starts `startLiveFollow`; no new live-follow UI or layout changes were added.

The first complete run encountered a pre-existing Android system ANR dialog that intercepted both touch-gesture tests. Diagnostic capture also found an Eval key-event timeout at 18:56:35, already present in this pass's initial `baseline-lastanr.txt`, and a system-process timeout at 19:05:53 while its ANR dialog was open. The earlier Eval trace is in Android's native input-event/JNI path and shows substantial CPU/memory/IO pressure; it does not identify a source-level cause. These records are retained in `data-app-anr.txt`, `system-anr.txt`, and `full-logcat.txt` rather than attributed to the current fixes.

Final verification for the seventh pass:

- The initial complete run finished with 74/76 passing in 142.694 seconds; both failures were input-injection errors caused by the system dialog. After rebooting the emulator and restoring Wi-Fi/DNS, the entire 76-test suite passed in 206.037 seconds (`settled-emulator-tests.txt`). This includes both gesture orientations, public retrieval from both providers, real Stockfish analysis, and all new regressions.
- Screenshot review then prompted only the variation result-bar correction. On that final APK, the existing nested-variation and ordinary full-analysis tests both passed in 42.741 seconds (`final-display-tests.txt`). `final-nested-variation.png` shows the correct board and “Variation · Move 2 · Black to move” label.
- All 20 JVM tests passed on the final build; debug/test APK builds and lint passed with zero errors and 88 warnings. `git diff --check` passed.
- Restored all 15 original files byte-for-byte from the fresh backup and removed only nine files created by this pass. The cold launch restored DrNykterstein–yoseph2013 at move 35/59 with live Stockfish analysis at depth 24 (`restored-game.png`).
- The installed APK and final local APK have the same SHA-256: `ecdb9148489fef04100e4fc7df1262f2c25b43a4759dba085935e79101b3ee94`. The shared cloud APK was not replaced because the previously reported automatic approval block still requires explicit authorization for that destination.
- No ANR occurred after the reboot, including the complete rerun, final display tests, and restored launch. The final app-process log contained no fatal exception or Stockfish/analysis error. The earlier timeout's underlying cause remains undetermined; this pass does not claim to have fixed it. Eval was left running with the user's original game, and no AI reports were generated.

## Lichess-only online retrieval and board shortcuts — 16 September 2026

Removed the retired online source's retrieval screens, network client, player/ranking/puzzle endpoints, username setting, app links, help text, and documentation references. Lichess remains the online source. Existing cached games from unknown or retired sources load as Local, retain their original payload keys across index updates, and page entirely from the cache. Saved PGN and analysed games remain intact. Unsupported saved reload settings and profile links no longer trigger online retrieval or appear in exported settings.

Manual-mode board double-taps now use the existing start/end navigation: the visible left half jumps to the start, and the right half to the last move. The same rules apply with a flipped board and while exploring a variation. Closely spaced source/target taps on different squares still move pieces; dragging and swiping retain their behavior.

Verification:

- Debug APK and instrumentation APK built successfully; all 35 JVM tests passed. Lint: zero errors, 88 existing warnings.
- All 24 focused emulator tests passed. The complete 99-test Android suite then passed in 347.174 seconds, including live Lichess retrieval, real Stockfish analysis, saved-game compatibility, settings import/export, and gesture interactions.
- Current source, documentation, and APK code/assets contain no references to the retired source.
- Restored the original files and preferences byte-for-byte after testing and removed only newly created test artifacts. Preserved Full screen enabled.
- On the restored game, actual left/right double-taps displayed Start position and Move 10/10 with +M0. Visually checked the retrieval screen with Lichess and all local options retained.
- Build, test logs, screenshots, and data backups: `/tmp/eval-lichess-double-tap-2026-09-16/`.

## Latest Lichess game at startup — 19 September 2026

Startup now retrieves one latest game for an explicitly saved Lichess username after Stockfish initializes. The configured username takes precedence over the last retrieved account; older preferences containing only a last Lichess account still work. A fresh install's example username does not cause automatic retrieval, and clearing the username disables it. When no account is known or retrieval fails, the last analysed game is restored when available, with retrieval errors preserved.

Startup reserves its request generation before engine initialization. Selecting, importing, clearing, or reloading another game while initialization or retrieval is pending prevents the late startup result from replacing that choice.

Validation: all 35 JVM tests passed, lint reported zero errors and 88 existing warnings, all 19 focused Android tests passed, and the full 107-test Android suite passed in 201.525 seconds. Original app data was restored byte-for-byte after tests. A real cold launch then issued the saved account's `max=1` request, received HTTP 200, opened the returned game, and began Stockfish analysis without user interaction. The final installed, local, and cloud APK hashes match. Evidence and backups are in `/tmp/eval-startup-latest-2026-09-19/`.
